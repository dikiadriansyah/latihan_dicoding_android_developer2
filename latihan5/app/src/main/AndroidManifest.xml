<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Latihan3"
        tools:targetApi="31">
        <activity
            android:name=".MoveForResultActivity"
            android:exported="false" />
        <activity
            android:name=".MoveWithObjectActivity"
            android:exported="false" />
        <activity
            android:name=".PindahDataActivity"
            android:exported="false" />
        <activity
            android:name=".PindahActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>

                <!--
              Intent-filter merupakan mekanisme untuk menentukan bagaimana suatu activity dijalankan
               oleh Android Runtime (ART) atau Dalvik Virtual Machine (DVM).
                -->
                <action android:name="android.intent.action.MAIN" />
                <!-- Baris di atas   bahwa MainActivity menjadi entry point ke aplikasi. -->

                <category android:name="android.intent.category.LAUNCHER" />
                <!--
Kode di atas menandakan bahwa MainActivity akan dikategorikan sebagai activity launcher.
Hal tersebut berarti activity ini akan muncul di halaman home screen dalam bentuk launcher.
                -->
            </intent-filter>
        </activity>
    </application>

</manifest>